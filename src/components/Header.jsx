/**
 * Header — Displays the app title and subtitle.
 */
const Header = () => {
    return (
        <header className="header">
            <h1>🌤️ Weather Dashboard</h1>
            <p>Live weather data for cities across India</p>
        </header>
    );
};

export default Header;
